/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is the new, correct way to enable static site generation
  output: 'export',
  
  // Optional: Add other configurations here if needed in the future
};

export default nextConfig;